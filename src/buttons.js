const Button = props => {
    return <button>{props.text}</button>;
};

const SubmitButton = () => {
    return <Button text="SUBMIT" />;
};

const LoginButton = () => {
    return <Button text="LOGIN" />;
};
