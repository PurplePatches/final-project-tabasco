import React from "react";

export default function() {
    return (
        <div>
            <h1>YOUR BIO</h1>
            <textarea />
            <button onClick={() => this.uploadBio()}>ADD BIO</button>
        </div>
    );
}
